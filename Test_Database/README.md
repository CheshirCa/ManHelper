# Local integration database

Place a local copy of the ManBase database here as `manbase.sqlite` before
running the Windows integration tests or `windows/manhelper/package.ps1`.

The database is deliberately ignored by Git because the current uncompressed
file is larger than GitHub's regular per-file limit. The packaged copy is
distributed inside the Windows ZIP through GitHub Releases.
